export interface Compra {
    producto: string; // Podría ser un objeto 'Producto' en lugar de un string
    cantidad: number;
    fecha: Date;
    imagen: string; // Ruta a la imagen del producto
  }
  